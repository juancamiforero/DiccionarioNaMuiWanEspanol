import { BuscarPalabrasPipe } from './buscar-palabras.pipe';

describe('BuscarPalabrasPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarPalabrasPipe();
    expect(pipe).toBeTruthy();
  });
});
